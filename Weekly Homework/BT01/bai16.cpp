#include <bits/stdc++.h>
using namespace std;

void bai16(){
    int a, b, c;
    cout << "Nhap vao so a: "; cin >> a;
    cout << "Nhap vao so b: "; cin >> b;
    cout << "Nhap vao so c: "; cin >> c;
    if(a == b && a == c) cout << "Ket qua: true" << endl;
    else cout << "Ket qua: false" << endl;
    return;
}
